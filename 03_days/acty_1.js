const num = 5;
if (num === 0 ) {
    console.log(`Number is ${num}`);
} else if (num > 0) {
    console.log(`${num} is positive`);
} else {
    console.log(`${num} is negative`);
} 

const age = 15;
if (age >= 18) {
    console.log(`The Number is ${age}, you are eligible to vote`);
} else {
    console.log(`The Number is ${age}, you are not eligible to vote `);
    }